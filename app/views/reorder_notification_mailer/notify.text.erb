Hi <%= @user.first_name %>,

The automated reorder check has created <%= @purchase_orders.count %> draft purchase order(s).

<% @purchase_orders.each do |po| %>
- <%= po.product.name %>: <%= po.product.current_stock %> on hand (reorder point: <%= po.product.reorder_point %>), ordering <%= po.quantity %> from <%= po.vendor&.name || 'TBD' %>
<% end %>

Review and approve at: <%= purchase_orders_url %>

— LoadNTrucks
