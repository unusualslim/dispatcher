Hi <%= @user.first_name %>,

The automated reorder check has created <%= @purchase_orders.count %> draft purchase order(s).

<% @purchase_orders.each do |po| %>
<% line = po.line_items.first; product = line&.product %>
- <%= product&.name || line&.display_name %>: <%= product&.current_stock %> on hand (reorder point: <%= product&.reorder_point %>), ordering <%= line&.quantity %> from <%= po.vendor&.name || 'TBD' %>
<% end %>

Review and approve at: <%= purchase_orders_url %>

— LoadNTrucks
