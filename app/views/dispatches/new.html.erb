<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <h1 class="mb-0">New Dispatch</h1>
    <div class="text-muted small">Fill in the details below to create a new dispatch</div>
  </div>
  <%= link_to "Back", dispatches_path, class: "btn btn-outline-secondary btn-sm", data: { turbo: false } %>
</div>

<%= render "form", dispatch: @dispatch %>
